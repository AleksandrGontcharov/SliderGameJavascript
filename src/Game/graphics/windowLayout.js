export let windowHeight = window.innerHeight;
export let windowWidth = Math.min(window.innerHeight * 0.8, window.innerWidth);
export let margin_x = 0.1;
export let margin_y = 0.07;
export let slider_padding = 0.025;
